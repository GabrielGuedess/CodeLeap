import { type StoryFn, type Meta } from '@storybook/react';

import { {{pascalCase name}} } from '.';

export default {
  title: '{{pascalCase type}}/{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta;

export const Default: StoryFn = () => <{{pascalCase name}} />;
